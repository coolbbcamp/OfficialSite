/** Background music wiring for App3D audio controller (class u3 init). */
export const backgroundMusic = {
  url: "children-loop.ogg",
  volume: 0.45,
};

/** Lower room bed so the music track is audible. */
export const roomAmbient = {
  volume: 0.2,
};
